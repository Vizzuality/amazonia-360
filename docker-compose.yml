version: '3.7'
services:
  api:
    build:
        context: ./api
        dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - amazonia360-network
    restart:
        always
networks:
  amazonia360-network:
    driver: bridge
